%tr.success
  %th{colspan:2}=title
  - @months.each do |month|
    %th= mmr_date(month)
- metrics.each do |trend| 
  %tr
    %th
    %th 
      #{trend.name}      
      <i class= 'icon-question-sign' rel="clickover" data-content= "#{trend.desc.html_safe}"></i> 
    - @months.each do |month|
      %td=human_data(trend,month) 
