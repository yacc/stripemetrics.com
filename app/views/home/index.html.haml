%h3 Console
%p
  %a(id='start_aggregation' title='test' href='#') start aggregation!
%p  
  %a(id='start_trend_report' title='test' href='#') start trend report!

%h3 Acquisition

#container{:style => "width:100%; height:400px;"}
  :javascript
    $(function () {
    var chart;
    $(document).ready(function() {
        chart = new Highcharts.Chart({
            chart: {
                renderTo: 'container',
                zoomType: 'x',
                spacingRight: 20
            },
            title: {
                text: 'Customer Acquisition'
            },
            subtitle: {
                text: document.ontouchstart === undefined ?
                    'Click and drag in the plot area to zoom in' :
                    'Drag your finger over the plot to zoom in'
            },
            xAxis: {
                type: 'datetime',
                maxZoom: 14 * 24 * 3600000, // fourteen days
                title: {
                    text: null
                }
            },
            yAxis: {
                title: {
                    text: '# Subscription'
                },
                showFirstLabel: false
            },
            tooltip: {
                shared: true
            },
            legend: {
                enabled: false
            },
            plotOptions: {
                area: {
                    fillColor: {
                        linearGradient: { x1: 0, y1: 0, x2: 0, y2: 1},
                        stops: [
                            [0, Highcharts.getOptions().colors[0]],
                            [1, 'rgba(2,0,0,0)']
                        ]
                    },
                    lineWidth: 1,
                    marker: {
                        enabled: false,
                        states: {
                            hover: {
                                enabled: true,
                                radius: 5
                            }
                        }
                    },
                    shadow: false,
                    states: {
                        hover: {
                            lineWidth: 1
                        }
                    },
                    threshold: null
                }
            },
    
            series: [{
                type: 'area',
                name: '#{@trend.name}',
                pointInterval: 24 * 3600 * 1000,
                pointStart: #{@trend.start_at},
                data: #{@trend.data}
            }]
        });
    });
    });

:javascript
  jQuery(document).ready(function() {
  	STRIPEMETRICS.job_control.init("#{Rails.env}",'#start_aggregation','aggregate_stripe_customer_data');
  	STRIPEMETRICS.job_control.init("#{Rails.env}",'#start_trend_report','customer_acquisition_trend');
  });

